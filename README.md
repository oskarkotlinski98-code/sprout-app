# sprout

A simple single-page React landing page, styled after AI "prompt to app" builders.

## Run it

```bash
npm install
npm run dev
```

Then open the local URL Vite prints (usually http://localhost:5173).

## Build for production

```bash
npm run build
npm run preview
```
